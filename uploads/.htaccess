# Las fotos subidas se sirven como archivos estáticos, pero nunca se
# debe poder ejecutar PHP acá adentro (por si alguien logra subir un
# archivo con extensión .php disfrazado de imagen).
<FilesMatch "\.(php|phtml|php[0-9]?)$">
    Require all denied
</FilesMatch>
